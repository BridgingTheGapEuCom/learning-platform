export interface User {
  email: string;
  firstName: string;
  lastName: string;
  organizations: any[];
  global_role: string;
  mustChangePassword: boolean;
  createdAt: string;
  updatedAt: string;
  id: string;
}
